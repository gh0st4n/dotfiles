# ===== CONFIGURE BSPWM =====

# ==== Apps ====
# == Terminal ==
super + Return
    alacritty

super + shift + Return
    xfce4-terminal

# == Rofi ==
# Menu
super + d
    rofi -show drun -config ~/.config/bspwm/utility/rofi/config.rasi

super + y
    rofi -show window -config ~/.config/bspwm/utility/rofi/workspace.rasi

super + shift + y
    ~/.config/bspwm/utility/scripts/rofi-monitor

super + p
    ~/.config/bspwm/utility/scripts/powermenu

super + shift + p
    ~/.configbspwm/utility/scripts/power-profiles
# == END

# == Apps ==
# File manager
super + e
    thunar

# Browser
super + w
    firefox

# Discord
super + shift + d
    discord

# Telegram
super + t
    telegram-desktop

# == Utility ==
# Screenshot GUI
Print
    flameshot gui
# Screenshot alternate
super + shift + s
    flameshot gui


# Wifi nmtui
super + shift + w
    alacritty -e nmtui


# Monitor
# Enable
super + n
    ~/.config/bspwm/utility/scripts/hdmi connect
# Disable
super + m
    ~/.config/bspwm/utility/scripts/hdmi disconnect


# AUDIO

XF86AudioRaiseVolume
    ~/.config/bspwm/utility/scripts/volume up

XF86AudioLowerVolume
    ~/.config/bspwm/utility/scripts/volume down

XF86AudioMute
    ~/.config/bspwm/utility/scripts/volume mute

XF86AudioMicMute
    amixer sset Capture toggle

super + shift + a
    ~/.config/bspwm/utility/scripts/audio-switch


# BRIGHTNESS

XF86MonBrightnessUp
    ~/.config/bspwm/utility/scripts/brightness up

XF86MonBrightnessDown
    ~/.config/bspwm/utility/scripts/brightness down

# == END ==


# == BSWPM ==
# Reload BSPWM
super + alt + r
    bspc wm -r

# Kill window
super + q
    bspc node -c

# Fullscreen
super + f
    bspc node -t fullscreen

super + shift + f
    bspc node -t tiled

# Floating toggle
super + space
    bspc node -t floating

super + shift + space
    bspc node -t ~floating

# Move focus
super + {h,j,k,l}
    bspc node -f {west,south,north,east}

# Swap windows
super + shift + {h,j,k,l}
    bspc node -s {west,south,north,east}

# Resize
# h & k
super + alt + {h,j,k,l}
    bspc node -z {left -20 0,bottom 0 20,top 0 -20,right 20 0}

# j & l
super + alt + shift + {h,j,k,l}
    bspc node -z {right -20 0,top 0 20,bottom 0 -20,left 20 0}

# == END ==


# == Workspace ==
# Workspaces BSPWM
super + {1,2,3,4,5,6}
    bspc desktop -f {1,2,3,4,5,6}

super + {7,8,9}
    bspc desktop -f Desktop{7,8,9}

# Move Workspace
super + shift + {1,2,3,4,5,6}
    bspc node -d {1,2,3,4,5,6}


# Switch workspace next/prev
super + Tab
    bspc desktop -f next

super + shift + Tab
    bspc desktop -f prev

# == END ==


